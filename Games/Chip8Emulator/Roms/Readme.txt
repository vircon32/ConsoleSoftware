These roms are Vircon32 memory card files.
Extract them from the ZIP file to use them.

They are distributed in compressed format
because each of them is 1MB in size when
extracted, but they only take a few KB of
space when compressed (since most of the
memory card space is not used).